services:
  - type: web
    name: fund-report
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 app_server.py --host 0.0.0.0"
    envVars:
      - key: APP_PASSWORD
        sync: false
